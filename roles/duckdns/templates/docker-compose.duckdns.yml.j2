---
version: '3'

services:
  duckdns:
    container_name: duckdns
    image: linuxserver/duckdns:{{ duckdns.version }}
    restart: unless-stopped
    environment:
      - TZ={{ common_timezone }}
      - SUBDOMAINS={{ domain }}
      - TOKEN={{duckdns.token}}
